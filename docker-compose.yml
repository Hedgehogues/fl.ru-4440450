version: '3'
services:
    parser:
        build:
            context: .
            dockerfile: Dockerfile
        volumes:
          - ./domains.csv:/app/domains.csv
          - ./existed.csv:/app/existed.csv
          - ./config.json:/app/config.json
        network_mode: host
